{
  "$schema": "https://schema.railpack.com/",
  "steps": {
    "build": {
      "commands": [
        "export JAVA_HOME=$(dirname $(dirname $(mise which java))) && chmod +x ./mvnw && ./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install"
      ]
    }
  }
}
