{
  "$schema": "https://schema.railpack.com/",
  "steps": {
    "build": {
      "commands": [
        "bash -c 'eval \"$(mise activate bash)\" && export JAVA_HOME=$(mise prefix java) && export PATH=\"$JAVA_HOME/bin:$PATH\" && java -version && chmod +x ./mvnw && ./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install'"
      ]
    }
  }
}
