{
  "$schema": "https://schema.railpack.com/",
  "steps": {
    "build": {
      "commands": [
        "unset JAVA_HOME && chmod +x ./mvnw && ./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install"
      ]
    }
  }
}
